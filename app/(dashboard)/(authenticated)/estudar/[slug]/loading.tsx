/**
 * Boundary de suspense do segmento `[slug]`.
 * O placeholder visual é renderizado pelo `EstudarQuestaoShell` para evitar dois cards empilhados.
 */
export default function LoadingQuestao() {
  return null;
}

diff --git a/e2e/audit-visual-baseline.spec.ts b/e2e/audit-visual-baseline.spec.ts
index 693ccecb..038a87af 100644
--- a/e2e/audit-visual-baseline.spec.ts
+++ b/e2e/audit-visual-baseline.spec.ts
@@ -3,7 +3,7 @@
  * Saída: docs/auditoria-visual-v2/screenshots/avant-baseline/
  *
  * Requer E2E_DASHBOARD_BYPASS (playwright.config webServer).
- * Executar: npx playwright test e2e/audit-visual-baseline.spec.ts --project=chromium --workers=1
+ * Executar: E2E_CAPTURE_MODE=true npx playwright test e2e/audit-visual-baseline.spec.ts --project=chromium --workers=1
  */
 import fs from 'fs';
 import path from 'path';
@@ -18,6 +18,11 @@ import {
 } from './helpers/vitrineE2e';
 import { expectLandingHeroVisible } from './helpers/landingE2e';
 import { expectGabaritoCorreto } from './helpers/playerE2e';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const OUT_DIR = path.join(process.cwd(), 'docs/auditoria-visual-v2/screenshots/avant-baseline');
 const BANCA_QUERY = encodeURIComponent(E2E_ESTUDAR_BANCA);
diff --git a/e2e/audit-visual-editorial-v2.spec.ts b/e2e/audit-visual-editorial-v2.spec.ts
index b88e9bc2..257c6975 100644
--- a/e2e/audit-visual-editorial-v2.spec.ts
+++ b/e2e/audit-visual-editorial-v2.spec.ts
@@ -4,9 +4,9 @@
  *
  * Requer E2E_DASHBOARD_BYPASS (playwright.config webServer ou dev com bypass).
  * Executar:
- *   npx playwright test e2e/audit-visual-editorial-v2.spec.ts --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true npx playwright test e2e/audit-visual-editorial-v2.spec.ts --project=chromium --workers=1
  * Com dev já rodando:
- *   PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-editorial-v2.spec.ts --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-editorial-v2.spec.ts --project=chromium --workers=1
  */
 import fs from 'fs';
 import path from 'path';
@@ -21,6 +21,11 @@ import {
 } from './helpers/vitrineE2e';
 import { expectLandingHeroVisible } from './helpers/landingE2e';
 import { expectGabaritoCorreto } from './helpers/playerE2e';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const BANCA_QUERY = encodeURIComponent(E2E_ESTUDAR_BANCA);
 
diff --git a/e2e/audit-visual-external.spec.ts b/e2e/audit-visual-external.spec.ts
index 03a1829b..968cfb9c 100644
--- a/e2e/audit-visual-external.spec.ts
+++ b/e2e/audit-visual-external.spec.ts
@@ -3,15 +3,15 @@
  * Saída: docs/auditoria-visual-v2/screenshots/{plataforma}/
  *
  * Público:
- *   PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts --project=chromium --workers=1
  *
  * Gabarita logado (credenciais em .env.local — não commitar):
  *   GABARITA_AUDIT_EMAIL=... GABARITA_AUDIT_PASSWORD=...
- *   PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts -g "Gabarita.*logad" --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts -g "Gabarita.*logad" --project=chromium --workers=1
  *
  * QConcursos vitrine logada (conta free):
  *   QCONCURSOS_AUDIT_EMAIL=... QCONCURSOS_AUDIT_PASSWORD=...
- *   PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts -g "QConcursos" --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true PLAYWRIGHT_SKIP_WEBSERVER=true npx playwright test e2e/audit-visual-external.spec.ts -g "QConcursos" --project=chromium --workers=1
  */
 import fs from 'fs';
 import path from 'path';
@@ -57,6 +57,11 @@ import {
   type QConcursosPageInventory,
 } from './helpers/qconcursosAuditMap';
 import { captureQConcursosPlayerFlow, openQConcursosUserMenu } from './helpers/qconcursosAuditPlayer';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 loadE2eEnv();
 
diff --git a/e2e/capture-desempenho-hub.spec.ts b/e2e/capture-desempenho-hub.spec.ts
index 57856058..889269d7 100644
--- a/e2e/capture-desempenho-hub.spec.ts
+++ b/e2e/capture-desempenho-hub.spec.ts
@@ -11,6 +11,11 @@ import { execSync } from 'child_process';
 import fs from 'fs';
 import path from 'path';
 import { expect, test, type Page } from '@playwright/test';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const VIEWPORTS = {
   mobile: { width: 390, height: 844 },
diff --git a/e2e/capture-editorial-premium-after.spec.ts b/e2e/capture-editorial-premium-after.spec.ts
index 01ccdca8..c019fed2 100644
--- a/e2e/capture-editorial-premium-after.spec.ts
+++ b/e2e/capture-editorial-premium-after.spec.ts
@@ -8,7 +8,7 @@
  * (3–4 progresso parcial/concluído dependem de dados reais — cobertos na vitrine enfermagem)
  *
  * Rodar:
- *   npx playwright test e2e/capture-editorial-premium-after.spec.ts --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true npx playwright test e2e/capture-editorial-premium-after.spec.ts --project=chromium --workers=1
  */
 import fs from 'fs';
 import path from 'path';
@@ -17,6 +17,11 @@ import {
   gotoVitrineE2e,
   vitrineStableLocalStorageInitScript,
 } from './helpers/vitrineE2e';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const OUT_DIR = path.join(
   process.cwd(),
diff --git a/e2e/capture-editorial-premium-before.spec.ts b/e2e/capture-editorial-premium-before.spec.ts
index 47fe6314..741eab56 100644
--- a/e2e/capture-editorial-premium-before.spec.ts
+++ b/e2e/capture-editorial-premium-before.spec.ts
@@ -6,7 +6,7 @@
  * Rotas: /estudar?disciplina=enfermagem, /progresso, /desempenho/simulados
  *
  * Rodar:
- *   npx playwright test e2e/capture-editorial-premium-before.spec.ts --project=chromium --workers=1
+ *   E2E_CAPTURE_MODE=true npx playwright test e2e/capture-editorial-premium-before.spec.ts --project=chromium --workers=1
  */
 import fs from 'fs';
 import path from 'path';
@@ -15,6 +15,11 @@ import {
   gotoVitrineE2e,
   vitrineStableLocalStorageInitScript,
 } from './helpers/vitrineE2e';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const OUT_DIR = path.join(
   process.cwd(),
diff --git a/e2e/capture-hero-mockups.spec.ts b/e2e/capture-hero-mockups.spec.ts
index ccc4f93a..2e811f4d 100644
--- a/e2e/capture-hero-mockups.spec.ts
+++ b/e2e/capture-hero-mockups.spec.ts
@@ -9,6 +9,11 @@ import fs from 'fs';
 import path from 'path';
 import { test, expect } from '@playwright/test';
 import { HERO_CAPTURE_DPR } from '../lib/marketing/heroCaptureDimensions';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const OUT_DIR = path.join(process.cwd(), 'public/mockups');
 const CAPTURE_URL = '/dev/hero-mockups';
diff --git a/e2e/capture-questao-review.spec.ts b/e2e/capture-questao-review.spec.ts
index e0857f3a..a30114c7 100644
--- a/e2e/capture-questao-review.spec.ts
+++ b/e2e/capture-questao-review.spec.ts
@@ -8,6 +8,11 @@
 import fs from 'fs';
 import path from 'path';
 import { test, expect } from '@playwright/test';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const slug = process.env.CAPTURE_QUESTAO_SLUG ?? '';
 const source = process.env.CAPTURE_QUESTAO_SOURCE ?? 'local';
diff --git a/e2e/capture-t3-vitrine.spec.ts b/e2e/capture-t3-vitrine.spec.ts
index 565bad55..bc622d67 100644
--- a/e2e/capture-t3-vitrine.spec.ts
+++ b/e2e/capture-t3-vitrine.spec.ts
@@ -6,6 +6,11 @@ import fs from 'fs';
 import path from 'path';
 import { test, type Page } from '@playwright/test';
 import { gotoVitrineE2e, vitrineStableLocalStorageInitScript } from './helpers/vitrineE2e';
+import { skipUnlessE2eCaptureMode } from './helpers/captureModeGate';
+
+test.beforeAll(() => {
+  skipUnlessE2eCaptureMode();
+});
 
 const OUT_DIR = path.join(
   process.cwd(),
diff --git a/e2e/mobile-drawer.spec.ts b/e2e/mobile-drawer.spec.ts
index f8cae647..7e0fdc7b 100644
--- a/e2e/mobile-drawer.spec.ts
+++ b/e2e/mobile-drawer.spec.ts
@@ -30,6 +30,15 @@ function maisButton(page: Page) {
   return bottomNav(page).getByRole('button', { name: /Abrir menu|Fechar menu/ });
 }
 
+/** BottomNav fica `pointer-events-none` enquanto welcome/modal bloqueia — espera interação real. */
+async function waitForBottomNavInteractive(page: Page) {
+  await page.waitForFunction(() => {
+    const nav = document.querySelector('nav[aria-label="Navegação rápida"]');
+    if (!nav) return false;
+    return window.getComputedStyle(nav).pointerEvents !== 'none';
+  });
+}
+
 /**
  * Drawer mobile (botão Mais). Viewport mobile forçado (CI usa só chromium/desktop).
  * Rodar: npm run test:e2e:drawer
@@ -162,14 +171,34 @@ test.describe('Dashboard — drawer mobile (Mais)', () => {
 
   test('D9 — drawer aberto: link Estudar inerte', async ({ page }) => {
     await page.goto('/ajuda', { waitUntil: 'domcontentloaded' });
+    await page.evaluate(() => {
+      try {
+        localStorage.setItem('avant.estudoReverso.welcomeShown', 'true');
+      } catch {
+        /* storage indisponível */
+      }
+    });
     await dismissWelcomeIfVisible(page);
-    await maisButton(page).click();
-    await expect(page.locator('#dashboard-mobile-drawer')).toBeVisible({ timeout: 10_000 });
+    await expect(page.getByRole('button', { name: 'Fechar introdução' })).not.toBeVisible({
+      timeout: 15_000,
+    });
+
+    const drawer = page.locator('#dashboard-mobile-drawer');
+    await expect(drawer).not.toBeVisible({ timeout: 5_000 });
+
+    await waitForBottomNavInteractive(page);
+    await expect(async () => {
+      if ((await maisButton(page).getAttribute('aria-expanded')) !== 'true') {
+        await maisButton(page).click();
+      }
+      await expect(maisButton(page)).toHaveAttribute('aria-expanded', 'true');
+    }).toPass({ timeout: 15_000 });
+    await expect(drawer).toBeVisible({ timeout: 10_000 });
 
     const estudarLink = bottomNav(page).locator('a[href="/estudar"]');
     await expect(estudarLink).toHaveAttribute('aria-hidden', 'true', { timeout: 10_000 });
     await expect(page).toHaveURL(/\/ajuda/);
-    await expect(page.locator('#dashboard-mobile-drawer')).toBeVisible({ timeout: 5_000 });
+    await expect(drawer).toBeVisible({ timeout: 5_000 });
   });
 
   test('D10 — rota do drawer: botão Mais com estado ativo', async ({ page }) => {
diff --git a/package.json b/package.json
index 381e7c82..1a26f2fa 100644
--- a/package.json
+++ b/package.json
@@ -13,6 +13,7 @@
     "typecheck": "tsc --noEmit",
     "test": "jest",
     "test:e2e": "playwright test",
+    "test:e2e:local": "tsx scripts/run-e2e-local.ts",
     "test:e2e:chromium": "playwright test --project=chromium",
     "test:e2e:modal": "tsx scripts/run-e2e-estudar-modal.ts",
     "test:e2e:modal:staging": "tsx scripts/run-e2e-estudar-modal.ts --target=staging",
@@ -41,10 +42,11 @@
     "setup:webhook": "bash scripts/setup-webhook.sh",
     "capture:tutorial": "tsx scripts/capture-tutorial-screens.ts",
     "capture:landing-neuroslides": "tsx scripts/capture-landing-neuroslides.ts",
-    "capture:hero-mockups": "playwright test e2e/capture-hero-mockups.spec.ts --project=chromium",
+    "capture:hero-mockups": "tsx scripts/capture-hero-mockups.ts",
     "copy:tutorial-assets": "powershell -ExecutionPolicy Bypass -File scripts/copy-user-tutorial-assets.ps1",
     "scale:dataset": "tsx scripts/generate-synthetic-dataset.ts",
     "perf:smoke": "tsx scripts/perf-smoke.ts",
+    "perf:smoke:local": "tsx scripts/run-perf-smoke-local.ts",
     "perf:smoke:staging": "tsx scripts/run-perf-smoke-staging.ts",
     "perf:baseline": "tsx scripts/capture-perf-baseline.ts",
     "perf:baseline:staging": "tsx scripts/capture-perf-baseline.ts --target=staging",
diff --git a/scripts/capture-desempenho-hub.ts b/scripts/capture-desempenho-hub.ts
index d7e94b3b..32313358 100644
--- a/scripts/capture-desempenho-hub.ts
+++ b/scripts/capture-desempenho-hub.ts
@@ -7,18 +7,20 @@
  */
 import { spawnSync } from 'node:child_process';
 
+import { buildManualCaptureWrapperEnv } from '@/lib/e2e/captureMode';
+
 const command =
   'npx playwright test e2e/capture-desempenho-hub.spec.ts --project=chromium --workers=1';
 
 const result = spawnSync(command, {
   stdio: 'inherit',
-  env: {
+  env: buildManualCaptureWrapperEnv({
     ...process.env,
     E2E_DASHBOARD_BYPASS: 'true',
     NEXT_PUBLIC_E2E_DASHBOARD_BYPASS: 'true',
     // Não herdar PLAYWRIGHT_TEST_BASE_URL de outro servidor (UI antiga / outra porta).
     PLAYWRIGHT_TEST_BASE_URL: 'http://localhost:3000',
-  },
+  }),
   shell: true,
   cwd: process.cwd(),
 });
diff --git a/scripts/capture-questao-review.ts b/scripts/capture-questao-review.ts
index 080eff06..0d1bd500 100644
--- a/scripts/capture-questao-review.ts
+++ b/scripts/capture-questao-review.ts
@@ -4,13 +4,9 @@
  *
  * Uso:
  *   npm run capture:questao-review -- --slug=idecan-...
- *   npm run capture:questao-review -- --slug=... --source=supabase
- *   npm run capture:questao-review -- --lote=imunizacao-g07
- *   npm run capture:questao-review -- --anchor-key=calendario_infantil
- *   npm run capture:questao-review -- --lote=imunizacao-g07 --viewport=mobile-375
  */
 import { spawnSync } from 'node:child_process';
-import { existsSync, mkdirSync } from 'node:fs';
+import { mkdirSync } from 'node:fs';
 import { resolve } from 'node:path';
 
 import {
@@ -18,6 +14,11 @@ import {
   resolveLoteReviewSlug,
 } from '@/lib/catalogMigration/captureLoteReview';
 import { parseArg } from '@/lib/catalogMigration/cliArgs';
+import { buildManualCaptureWrapperEnv } from '@/lib/e2e/captureMode';
+import {
+  findNewOrModifiedPngs,
+  snapshotPngFiles,
+} from '@/lib/harness/captureFreshOutput';
 
 function resolveSlug(): { slug: string; source: string; reason: string } {
   const explicitSlug = parseArg('slug');
@@ -52,6 +53,7 @@ function main(): void {
   const viewport = parseArg('viewport') ?? 'desktop';
   const outDir = resolve(process.cwd(), 'artifacts/questao-review', slug);
   mkdirSync(outDir, { recursive: true });
+  const pngBefore = snapshotPngFiles(outDir);
 
   const specArgs = [
     'playwright',
@@ -61,13 +63,12 @@ function main(): void {
     `--grep=${slug}`,
   ];
 
-  const env = {
-    ...process.env,
+  const env = buildManualCaptureWrapperEnv(process.env, {
     CAPTURE_QUESTAO_SLUG: slug,
     CAPTURE_QUESTAO_SOURCE: source,
     CAPTURE_QUESTAO_OUT_DIR: outDir,
     CAPTURE_QUESTAO_VIEWPORT: viewport,
-  };
+  });
 
   console.log(`[capture:questao-review] slug=${slug} source=${source} (${reason})`);
   console.log(`[capture:questao-review] viewport=${viewport}`);
@@ -86,9 +87,18 @@ function main(): void {
     return;
   }
 
-  if (existsSync(outDir)) {
-    console.log(`[capture:questao-review] PNGs em ${outDir}`);
+  const freshPngs = findNewOrModifiedPngs(outDir, pngBefore);
+  if (freshPngs.length === 0) {
+    console.error(
+      '[capture:questao-review] Nenhum PNG novo ou modificado nesta execução — stale output ou spec skipped',
+    );
+    process.exitCode = 1;
+    return;
   }
+
+  console.log(
+    `[capture:questao-review] ${freshPngs.length} PNG(s) fresh em ${outDir}: ${freshPngs.join(', ')}`,
+  );
 }
 
 main();
diff --git a/scripts/capture-t3-vitrine.ts b/scripts/capture-t3-vitrine.ts
index 781a3877..0aa1a986 100644
--- a/scripts/capture-t3-vitrine.ts
+++ b/scripts/capture-t3-vitrine.ts
@@ -1,17 +1,19 @@
 #!/usr/bin/env tsx
 import { spawnSync } from 'node:child_process';
 
+import { buildManualCaptureWrapperEnv } from '@/lib/e2e/captureMode';
+
 const command =
   'npx playwright test e2e/capture-t3-vitrine.spec.ts --project=chromium --workers=1';
 
 const result = spawnSync(command, {
   stdio: 'inherit',
-  env: {
+  env: buildManualCaptureWrapperEnv({
     ...process.env,
     E2E_ADMIN_BYPASS: 'true',
     E2E_DASHBOARD_BYPASS: 'true',
     NEXT_PUBLIC_E2E_DASHBOARD_BYPASS: 'true',
-  },
+  }),
   shell: true,
   cwd: process.cwd(),
 });

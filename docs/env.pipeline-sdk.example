# Cursor SDK — orquestrador de pipeline (copiar para .env.local na raiz do repo)
# Dashboard: https://cursor.com/dashboard → Integrations / API Keys
# Nunca commitar .env.local (já está no .gitignore)

CURSOR_API_KEY=cursor_COLE_SUA_CHAVE_AQUI

# Opcional — default do orquestrador: composer-2.5
CURSOR_ORCHESTRATOR_MODEL=composer-2.5
